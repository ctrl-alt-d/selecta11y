{
  "name": "selecta11y",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel serve src/index.html --public-url / --dist-dir docs",
    "build": "parcel build --public-url ./ --dist-dir docs"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.3",
    "parcel": "latest"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.2"
  }
}
